# Flask User API

Flask + SQLite asosida mini REST API.

## Install
pip install -r requirements.txt

## Run
python app.py

## Endpoints

GET /users  
POST /users  
DELETE /users/<id>

## Example POST body (JSON)
{
  "name": "Ali",
  "age": 25
}
